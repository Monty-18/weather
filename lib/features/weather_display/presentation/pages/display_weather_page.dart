import 'package:flutter/material.dart';

class DisplayWeatherPage extends StatelessWidget {
  const DisplayWeatherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
